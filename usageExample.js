// Example usage:
const calculateCalories = require("./index");
const sugarAmount = 50; // Replace with the actual amount of sugar in grams
const calories = calculateCalories(sugarAmount);

console.log(`Calories for ${sugarAmount} grams of sugar: ${calories} kcal`);
